{
  "name": "spatial-mindset-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd SpatialMind_Frontend && npm install && npm run build",
    "install": "npm install && cd SpatialMind_Frontend && npm install"
  },
  "devDependencies": {
    "vite": "^8.0.4"
  }
}
